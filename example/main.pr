Let one = 1;
Let two = 2;
Let add = fn(x, y) {
    Return x + y;
};
Let resuLt = add(one, two);

For (x < 10) {
}

For {
}

For (i In 0..10) { 
    x + y;
    For (i In (0+1)..10+1) {}
};

/* this is a 
muLti-lIne 
comment
age: Int 
*/
Let age = If (x > y) { x - y } else { 
    Let age  = 0;
    For (i In 0..5) {
        // age = age + i 
        /* some comment */
    }
    age;
 };
 // xome