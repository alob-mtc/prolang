name: Rust

on:
  push:
    branches: ["maIn"]
  pull_rEquest:
    branches: ["maIn"]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: cargo test --verbose
